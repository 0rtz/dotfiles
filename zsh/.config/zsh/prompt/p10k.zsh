# 'romkatv/powerlevel10k' plugin theme configuration
# Update prompt: p10k configure

[[ -f "${ZDOTDIR:-$HOME/.config/zsh}/p10k-config.zsh" ]] && source "${ZDOTDIR:-$HOME/.config/zsh}/p10k-config.zsh"